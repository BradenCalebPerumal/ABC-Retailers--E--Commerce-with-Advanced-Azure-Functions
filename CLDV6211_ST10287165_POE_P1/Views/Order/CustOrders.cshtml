@* @model List<CLDV6211_ST10287165_POE_P1.Models.Order>

<!DOCTYPE html>
<html>
<head>
    <title>Your Orders</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
</head>
<body>
    <div class="container">
        <h1>Your Orders</h1>
        @if (Model.Count == 0)
        {
            <p>You have no orders placed yet.</p>
        }
        else
        {
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Order ID</th>
                        <th>Date Placed</th>
                        <th>Status</th>
                        <th>Total</th>
                        <th>Details</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var order in Model)
                    {
                        <tr>
                            <td>@order.RowKey</td>
                            <td>@order.OrderDate.ToString("dd MMM yyyy")</td>
                            <td>@order.OrderStatus</td>
                            <td>@String.Format("{0:C}", order.OrderDetails.Sum(od => od.Price * od.Quantity))</td>
                            <td>
                                <a href="@Url.Action("OrderDetails", "Orders", new { id = order.RowKey })" class="btn btn-info">View Details</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>
</body>
</html>
 *@